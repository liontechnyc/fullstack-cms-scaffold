{
  "host": "${process.env.APP_ID}.herokuapp.com",
  "port": "${process.env.PORT || 8081}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false,
    "path": "/dashboard"
  }
}

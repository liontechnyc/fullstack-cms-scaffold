{
    "name": "lts-cms-stack",
    "private": true,
    "description": "An application stack for rapidly deploying fullstack CMS solutions",
    "version": "0.1.0",
    "author": "Ezra Singh <ezra@liontech.nyc>",
    "dependencies": {},
    "license": "MIT",
    "scripts": {
        "build:frontend" : "rm -rf frontend/public && npm --prefix frontend run build",
        "staging": "npm run build:frontend && docker-compose -f docker-compose.yml -f docker-compose.staging.yml up --build",
        "production": "docker-compose -f docker-compose.yml -f docker-compose.prod.yml up --build",
        "docker:down": "docker-compose down"
    },
    "repository": {
        "type": "git",
        "url": "https://gitlab.com/liontechnyc/inergy/cms-stack"
    },
    "bugs": {
        "url": "https://gitlab.com/liontechnyc/inergy/cms-stack"
    }
}
